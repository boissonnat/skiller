<% if can? :read, Question %>
    <li><%= link_to 'Questions', questions_path %></li>
<% end %>

<% if can? :list, Quiz %>
    <li class="dropdown">
      <a href="#" class="dropdown-toggle" data-toggle="dropdown">
        Quizzes <b class="caret"></b>
      </a>
      <ul class="dropdown-menu">
        <li><%= link_to icon('list') + ' All quizzes', quizzes_path %></li>
        <li><%= link_to icon('gears') + ' Generate quizzes', new_quiz_path %></li>
      </ul>
    </li>
<% end %>